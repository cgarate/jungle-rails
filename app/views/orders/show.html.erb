<section class="orders-show">

  <header class="page-header">
    <h1>Order #<%= @order.id %></h1>
  </header>

  <div class="order-summary jumbotron">
    <h4>Thank you for your order <%= current_user.name %>!</h4>

    <table class="table table-bordered">
      <thead>
        <tr>
          <th colspan="2">Product</th>
          <th>Qty Ordered</th>
          <th>Price</th>
        </tr>
      </thead>
      <tbody>
        <% line_items = LineItem.where(order_id: @order.id) %>
        <% line_items.each do |oli| %>
          <%= render 'orders/orderlineitem', li: oli %>
        <% end %>
        Your order total is: <%= humanized_money_with_symbol @order.total %> <br/>
        An email has been sent to: <%= current_user.email %>. <br/>
      </tbody>
    </table>

  </div>

  <%= link_to "Continue Shopping", root_path, class: 'btn btn-lg btn-primary' %>

</section>
